# ローカル検証用の kind クラスタ設定。
# いまは最小構成（control-plane 1台）で、セットアップ時間と消費リソースを抑える。
# 将来、ポート公開や worker 追加が必要になったらこのファイルに追記して管理する。
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
